<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="#f4f4f4"
    android:weightSum="1">

    <!-- Layout for the specific field info in the fields list part of the app -->
    <ExpandableListView
        android:id="@+id/specific_field_info_expandable_listview"
        android:layout_height="495dp"
        android:layout_above="@+id/specific_field_info_home_button"
        android:layout_width="fill_parent"
        android:layout_weight="1" />

    <Button android:id="@+id/specific_field_info_home_button"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="Home"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:textSize="18sp"
        android:layout_gravity="center_horizontal" />
</LinearLayout>