Credential Storage
	Investigate AccountManager
		Makes app account appear in Settings menu

	Investigate SharedCredentials
		Less secure, but easier to use
		Would need to be paired with encryption

Fields
	Store fields in a class for easy comparison
	Cache changes in local storage
		Compare local cache to server's version; if different, sync local
		What if local overwrites changes on website?

	
Pest Sampler
	Should copy algorithm locally to reduce connectivity
	